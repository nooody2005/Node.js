// console.log('hello from the client side :D');

// const locations = JSON.parse(document.getElementById('map').dataset.locations);      // will be moved in index.js 
// console.log(locations);

export const displayMap = (locations) => {
    // all contents from map video will be moved in here
};